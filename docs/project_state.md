# Project state

- CURRENT STAGE: bounded generic Mamba-2 integration validated; stopped before training-infrastructure implementation.
- CURRENT MODEL SCALE: **1,929,579 actual unique trainable parameters** = byte hierarchy 201,483 + shared Mamba 1,728,096. All 36 shared tensors and all component counts match inventory exactly. Target remains 2,000,000 with unchanged 2% tolerance; -70,421 (-3.52105%) is the explicit accepted smoke-stage exception, not WITHIN_TARGET.
- LAST VERIFIED / STARTING BYTE-HIERARCHY COMMIT: **78c179bda571237d3c0d9aa6ec396bf3f6374b0b**. Approved byte diff reviewed, all 118 tests and static/dependency checks rerun, committed as one coherent tranche; working tree clean before Mamba implementation. Current Mamba changes remain uncommitted for review.
- CONFIGURATION FOUNDATION COMMIT: 5265179e84e3b3fcc38eb22528fcb349c78f7fee, preserved without amend/rewrite. No remote added.
- GIT/ROLLBACK: command-local safe.directory=E:/Theai permits normal index/object/ref writes on existing master. Old HEAD replacement and ACL edits remain Windows-restricted but are unnecessary for the verified commit path. No global Git, ACL, antivirus or system-security changes. The approved byte commit is the rollback boundary; the original ignored configuration archive and hashes remain available.
- PASSING TESTS: **143 passed, 0 failed/skipped**, including all unchanged 118 accepted cases, in 61.611 seconds. Ruff lint/format, compileall/import execution and pip check passed. README examples, complete diff/whitespace review, source hashes, artifact exclusions and rollback/specification integrity checks passed. XML is preserved under evidence/mamba_integration/full_suite.xml; reproducible numerical collector: tests/measure_mamba.py.
- NUMERICAL GATES: shared full/step max absolute error 3.2186508178710938e-6; integrated full/step 2.2649765014648438e-6; FP64 equation oracle 1.8850363225908495e-6. Adversarial future-suffix prefix differences and serialized identical-path continuation/state differences are zero. All combined atol=1e-5, rtol=1e-4 gates pass; relative maxima near zero references are reported honestly in JSON/report, with no tolerance change.
- CANONICAL STATE: four typed layer states, each convolution [B,640,4] and SSM [B,8,64,64], CPU FP32. Measured combined tensor/backing-storage bytes: B=1 565,248; B=2 1,130,496; B=4 2,260,992. Corrected headdim-aware planning formula matches. These are canonical recurrent allocations, not process RSS, autograd-history or activation memory.
- INTEGRATION: learned BOS passes through all four shared layers before first-patch local decoding. Only complete eight-symbol patches update Mamba, exactly once per consume transition. Partial patches remain local. The wrapper owns event processing; shared.steps=1+completed_patches. Reset removes prior context and processes fresh BOS. Tagged snapshots validate config/schema, batch, shape, dtype/device, finite state and clocks; restore requires identical weights.
- GRADIENT EVIDENCE: all 56 trainable tensors have present, finite, nonzero-norm gradients across three controlled 25-symbol cross-entropy fixtures. Independent full/step gradient comparison passes. No optimizer, dataset, pretraining pipeline or learning-quality claim.
- FAILED TESTS / FIXES: no remaining failures. A new regression exposed finite-factor overflow in dt*(-exp(A_log)); explicit layer-labelled finite checks now reject it, including sequence accumulations, without clamping. Import/format lint findings fixed. No accepted byte/config implementation or test was changed.
- DEPENDENCIES / REFERENCE: tested project-local Python 3.12.14, torch 2.6.0+cpu, PyYAML 6.0.2, pytest 8.3.5, Ruff 0.11.13 and existing transitive lock unchanged. Semantic reference remains state-spaces/mamba v2.2.6 / d7b1ceb3c367ec9022925e812f507bcf706937c6, an engineering pin, not a Bible mandate. Exact source hashes, retrieval date, equations and subset are documented.
- WARNINGS / UNVERIFIED: NumPy optional bridge remains absent and its warning visible; used tensor/autograd/serialization paths passed. Upstream package-runtime parity is UNVERIFIED/BLOCKED because mamba_ssm and Triton are absent on this Windows CPU profile; independent mathematical/source-semantic parity is measured. GPU/reduced precision, fresh/cross-platform installation, long-context retention and learning behavior are unvalidated. Overall runtime memory remains UNKNOWN_REQUIRES_MEASUREMENT. The full SSD reference path uses O(T^2) storage; use bounded sequences/chunks and inference_mode/no_grad for streaming inference.
- ARCHITECTURE / SPECIFICATIONS: no new accounting mismatch or architectural conflict. Both FINAL documents remain byte-identical; AC-001..005 and the resolved candidate remain unchanged. Same-chunk MoE routing is still deferred. No MoE, router, tools, RAG, quantization, custom kernels, latent reasoning, MTP or later model sizes were added.
- BLOCKERS: none for implementation of bounded CPU training infrastructure. Upstream package-runtime parity and actual process/peak memory remain explicit evidence gaps, not claims of successful runtime validation. The usage-review interruption was resumed under the user's directive; no interrupted test write was lost or duplicated.
- NEXT PROMOTION GATE: implement training infrastructure only when separately authorized, preserving causal state ownership, no-weight-decay metadata, memory checks and strict optimizer/checkpoint semantics. Training readiness, long runs and later-size promotion remain separate gates.
- FINAL DECISION: READY FOR TRAINING INFRASTRUCTURE; stopped at this tranche boundary.

Detailed evidence: reports/mamba_integration_readiness.md and reports/mamba_integration_evidence.json. Contract: docs/mamba_integration_contract.md. Earlier accepted reports remain historical evidence for their own completed boundaries.
