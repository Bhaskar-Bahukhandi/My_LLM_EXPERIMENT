# Project state

- CURRENT STAGE: byte hierarchy implemented and validated; stopped before Mamba integration.
- CURRENT MODEL SCALE: full structural inventory 1,929,579 parameters; target 2,000,000, delta -70,421 (-3.52105%). OUTSIDE_TARGET remains an explicit accepted smoke-stage exception; nominal 2% tolerance and resolved snapshot unchanged. Executable byte components total 201,483; shared 1,728,096 remains inventory only.
- LAST VERIFIED COMMIT: 5265179e84e3b3fcc38eb22528fcb349c78f7fee (clean configuration foundation on existing master, no remote). All 29 committed files verified against the pre-byte archive plus reviewed .gitignore. Byte-hierarchy continuation is uncommitted for review.
- GIT/ROLLBACK: normal index/object/ref writes verified using command-local safe.directory=E:/Theai. Existing HEAD replacement and ACL editing remain denied by Windows ownership/permissions; neither operation is required to commit on master. No ACL, global Git, antivirus or system-security changes were made. The first narrow ACL attempt was denied without changing its recorded SDDL. The foundation commit and ignored evidence/byte_hierarchy/rollback/configuration_foundation.zip preserve rollback; archive SHA-256 792bb2e693a7276e9bdaf8718df3348e87706f45654d424242d6ba8d11698627.
- PASSING TESTS: 118 passed, 0 failed/skipped, including all unchanged 81 foundation cases. Ruff lint/format, compileall and pip check pass. Raw binary/control contracts, partial/completed patches, learned BOS, independent local equations, adversarial future suffixes, state serialization, finite gradients and exact partial parameter reconciliation verified on FP32 CPU.
- NUMERICAL EVIDENCE: three seeds; maximum adversarial prefix and restored-prefix logit error 0; maximum independent GRU logit error 9.5367431640625e-7. Every encoder patch position has nonzero gradient influence. reports/byte_hierarchy_evidence.json records results and source hashes.
- FAILED TESTS: no remaining failures. A first encoder endpoint implementation ignored positions 0..4; independent tests exposed it and it was corrected to mean pooling with unchanged parameters. An explicit-context boolean batch validation defect was also reproduced and fixed. Standard sandbox command launch still fails; narrow reviewed workspace commands were used.
- KNOWN ISSUES: memory preflight remains UNKNOWN_REQUIRES_MEASUREMENT. Optional NumPy bridge is unavailable and emits one warning; tensor execution, autograd and weights/state save/restore work without it. Fresh environment recreation, non-Windows execution, CUDA, full-model runtime memory and training are unvalidated.
- BYTE CONTRACT: versioned 267-symbol registry unchanged. Raw bytes 0..255; PAD storage only; BOS out-of-band learned request conditioning; remaining controls occupy slots. Only eight observed symbols emit a completed patch. The hierarchy then requires real external conditioning; no substitute shared trunk exists. Synchronous batches require caller grouping for ragged lengths. Saved local state requires identical weights and caller-owned outbound event/external context at a boundary.
- ARCHITECTURE CHANGES: FINAL Bible/Roadmap unchanged. AC-001/002/004/005 remain adopted; AC-003 same-chunk routing remains deferred. No new specification deviation. Mean pooling corrects an implementation defect, not a FINAL specification. Engineering semantic reference v2.2.6 / d7b1ceb3c367ec9022925e812f507bcf706937c6 was locally reverified; it is neither a Bible version mandate nor an installed dependency.
- ACTIVE EXPERIMENTS: none. No training, dataset, checkpoint or Mamba implementation added.
- BLOCKERS: none for byte-hierarchy integration readiness. Full executable count, upstream Mamba numerical parity and runtime memory require the next separately authorized tranche. The absent shared trunk is an intentional scope boundary.
- NEXT PROMOTION GATE: integrate the pinned generic Mamba-2 semantics only when authorized; process each completed event exactly once, enforce causal conditioning, own external recurrent state, reconcile shared tensors and measure runtime memory. No training readiness or later-size promotion is claimed.
- FALLBACK/ROLLBACK: original specifications and accepted config/source/tests/dependencies preserved. No existing behavior removed. Use the foundation commit for a separate rollback checkout; keep uncommitted byte additions until review. Ignored local archive provides additional byte-for-byte evidence.

Detailed evidence: reports/byte_hierarchy_readiness.md and reports/byte_hierarchy_evidence.json. The configuration foundation report remains authoritative for its unchanged scope.
